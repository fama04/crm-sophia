import { CodePostalPipe } from './code-postal.pipe';

describe('CodePostalPipe', () => {
  it('create an instance', () => {
    const pipe = new CodePostalPipe();
    expect(pipe).toBeTruthy();
  });
});
